console.log(null > 0);
console.log(null == 0);
console.log(null>=0);

console.log(undefined == 0);
// in most of the cases we will avoid these types of conversions.
// undefined always returns false in all cases.

// == is only for conversion
// === will check values strictly and also check its datatypes.